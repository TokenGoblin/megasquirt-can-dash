# Pre-refactor snapshot - kept locally, never published or edited.
backup/

# NOTE: /lib is deliberately NOT ignored - but don't commit libraries either.
# Users copy the required Adafruit bundle libraries into CIRCUITPY/lib per the
# README's "CircuitPython setup" table (names + tested versions listed there).

# OS cruft
.DS_Store
._*
.Trashes
Thumbs.db
desktop.ini
System Volume Information/

# Editors
.vscode/
.idea/
*.swp

# Python
*.pyc
__pycache__/
